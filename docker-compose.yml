services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: descale-web
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  # Optional dev server (uncomment to use)
  # dev:
  #   image: node:20-alpine
  #   working_dir: /app
  #   command: sh -c "npm install && npm run dev -- --host"
  #   ports:
  #     - "4028:4028"
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
